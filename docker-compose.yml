services:
    web:
        build:
            context: .
        image: tame_web
        container_name: "tame-web"
        command: ['gunicorn', '--bind', '0.0.0.0:5000', 'tame:create_app()']
        expose:
            - 5000
        volumes:
            - ./auth.db:/opt/tame/auth.db
            - ${TAMENOTESDIR:-"./README.md"}:/etc/notes
        networks:
            tame_net:
                ipv4_address: 172.49.234.3
    nginx:
        build:
            context: ./nginx
        image: tame_nginx
        container_name: "tame-nginx"
        ports:
            - 80:80
            - 443:443
        depends_on:
            - web
        networks:
            tame_net:
                ipv4_address: 172.49.234.2
        volumes:
            - ./nginx/tame.crt:/etc/nginx/tame.crt:ro
            - ./nginx/tame.key:/etc/nginx/tame.key:ro
            - ./nginx/dhparam.pem:/etc/nginx/dhparam.pem:ro
networks:
    tame_net:
        name: tame_network
        ipam:
            driver: default
            config:
                - subnet: "172.49.234.0/24"
